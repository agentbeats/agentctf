services:
  attack:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lobechat-1.136.1-attacker
    working_dir: /workspace
    volumes:
      - lobechat-source-1136:/app/lobechat-source:ro
    environment:
      - TARGET_URL=http://lobechat:3010
    networks:
      - exploit_net

networks:
  exploit_net:
    driver: bridge
