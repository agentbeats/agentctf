services:
  attack:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lobechat-1.129.3-attacker
    working_dir: /workspace
    volumes:
      - lobechat-source-1129:/app/lobechat-source:ro
    environment:
      - TARGET_URL=http://lobechat:3000
    networks:
      - exploit_net

networks:
  exploit_net:
    driver: bridge
