services:
  attack:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lobechat-0.122.3-attacker
    working_dir: /workspace
    volumes:
      - lobechat-source-0122:/app/lobechat-source:ro
    environment:
      - TARGET_URL=http://lobechat:3210
    networks:
      - exploit_net

networks:
  exploit_net:
    driver: bridge
