services:
  attack:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: langchain-0.0.246-attacker
    working_dir: /workspace
    volumes:
      - langchain-source-0246:/app/langchain-source:ro
    environment:
      - TARGET_URL=http://langchain:8080
    networks:
      - exploit_net

networks:
  exploit_net:
    driver: bridge
